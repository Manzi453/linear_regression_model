# TODO: Fix Render Deployment

## Steps:
- [x] Step 1: Update requirements.txt files with compatible ~= versions
- [x] Step 2: Test locally: pip install -r requirements.txt
- [x] Step 3: Test API: cd summative/API && uvicorn main:app --host 0.0.0.0 --port 8000 --reload
- [ ] Step 4: Commit and push: git add . && git commit -m "Fix Render deploy: compatible requirements" && git push
- [ ] Step 5: Verify Render logs show successful build

Progress will be updated after each step.

- [ ] Step 3: Test API: cd summative/API && uvicorn main:app --host 0.0.0.0 --port 8000 --reload
- [ ] Step 4: Commit and push: git add . && git commit -m "Fix Render deploy: compatible requirements" && git push
- [ ] Step 5: Verify Render logs show successful build

Progress will be updated after each step.

- [ ] Step 2: Test locally: pip install -r requirements.txt
- [ ] Step 3: Test API: cd summative/API && uvicorn main:app --host 0.0.0.0 --port 8000 --reload
- [ ] Step 4: Commit and push: git add . && git commit -m "Fix Render deploy: compatible requirements" && git push
- [ ] Step 5: Verify Render logs show successful build

Progress will be updated after each step.

